## Системные паттерны

- Используется подход с микропроектами для изоляции изучаемых тем.
- Для каждого микропроекта в `readme.md` в секции `<microprojects-list>` задаются:
  - кодовое имя,
  - путь к директории,
  - текстовое описание.

